# OTH-S-game-
Catch the star 0.1
<!DOCTYPE html> <html> <head>   <title>OTH-S ✦ Game</title>   <style>     * {       margin: 0;       padding: 0;       box-sizing: border-box;     }          body {       text-align: center;       font-family: Arial, sans-serif;       background: #0d0d1a;       color: white;       height: 100vh;       display: flex;       flex-direction: column;       justify-content: center;       align-items: center;       overflow: hidden;     }          h1 {       font-size: 3em;       margin-bottom: 20px;       text-shadow: 0 0 10px rgba(255, 215, 0, 0.8);       letter-spacing: 2px;     }          #gameContainer {       position: relative;       width: 800px;       height: 600px;       background: rgba(20, 20, 40, 0.9);       border: 2px solid gold;       border-radius: 10px;       overflow: hidden;       box-shadow: 0 0 20px rgba(255, 215, 0, 0.5);     }          #star {       font-size: 50px;       position: absolute;       cursor: pointer;       user-select: none;       transition: transform 0.1s;     }          #star:hover {       transform: scale(1.2);       filter: drop-shadow(0 0 10px gold);     }          #score {       position: absolute;       top: 20px;       right: 20px;       font-size: 24px;       font-weight: bold;       color: gold;       text-shadow: 0 0 10px rgba(255, 215, 0, 0.8);     }          #gameOver {       position: absolute;       top: 50%;       left: 50%;       transform: translate(-50%, -50%);       background: rgba(0, 0, 0, 0.95);       padding: 40px;       border-radius: 10px;       display: none;       z-index: 100;       text-align: center;       border: 2px solid gold;     }          #gameOver h2 {       font-size: 2em;       margin-bottom: 20px;       color: gold;     }          #finalScore {       font-size: 1.5em;       margin: 20px 0;       color: #90EE90;     }          button {       background: gold;       color: black;       border: none;       padding: 12px 30px;       font-size: 1em;       border-radius: 5px;       cursor: pointer;       font-weight: bold;       margin-top: 20px;       transition: background 0.3s;     }          button:hover {       background: #FFD700;       transform: scale(1.05);     }          #timer {       position: absolute;       top: 20px;       left: 20px;       font-size: 24px;       font-weight: bold;       color: #FF6B6B;       text-shadow: 0 0 10px rgba(255, 107, 107, 0.8);     }   </style> </head> <body>   <h1>✦ OTH-S GAME ✦</h1>      <div id="gameContainer">     <div id="timer">Time: <span id="timeLeft">30</span>s</div>     <div id="score">Score: <span id="scoreValue">0</span></div>     <div id="star">⭐</div>          <div id="gameOver">       <h2>GAME OVER!</h2>       <div id="finalScore">Final Score: <span id="finalScoreValue">0</span></div>       <button onclick="restartGame()">Play Again</button>     </div>   </div>    <script>     const star = document.getElementById('star');     const scoreValue = document.getElementById('scoreValue');     const timeLeft = document.getElementById('timeLeft');     const gameOverScreen = document.getElementById('gameOver');     const finalScoreValue = document.getElementById('finalScoreValue');     const gameContainer = document.getElementById('gameContainer');          let score = 0;     let timeRemaining = 30;     let gameActive = true;          function getRandomPosition() {       const containerRect = gameContainer.getBoundingClientRect();       const starSize = 50;              const randomX = Math.random() * (gameContainer.offsetWidth - starSize);       const randomY = Math.random() * (gameContainer.offsetHeight - starSize);              return { x: randomX, y: randomY };     }          function moveStar() {       if (!gameActive) return;              const position = getRandomPosition();       star.style.left = position.x + 'px';       star.style.top = position.y + 'px';     }          function clickStar(e) {       if (!gameActive) return;              score++;       scoreValue.textContent = score;       moveStar();              // Visual feedback       star.style.transform = 'scale(1.3)';       setTimeout(() => {         star.style.transform = 'scale(1)';       }, 100);     }          function updateTimer() {       timeRemaining--;       timeLeft.textContent = timeRemaining;              if (timeRemaining <= 0) {         endGame();       }     }          function endGame() {       gameActive = false;       star.style.display = 'none';       finalScoreValue.textContent = score;       gameOverScreen.style.display = 'block';     }          function restartGame() {       score = 0;       timeRemaining = 30;       gameActive = true;       scoreValue.textContent = score;       timeLeft.textContent = timeRemaining;       gameOverScreen.style.display = 'none';       star.style.display = 'block';              moveStar();       startGame();     }          function startGame() {       moveStar();       const timerInterval = setInterval(() => {         if (!gameActive) {           clearInterval(timerInterval);         } else {           updateTimer();         }       }, 1000);     }          star.addEventListener('click', clickSptar);     ();   </script> </bo
